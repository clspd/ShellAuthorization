Permission Explanation

To provide better user experience, we will use the following permissions:

<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
We will post notifications when TODO: write usages to TODO: write the purpose

<uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
We will show system alert windows when TODO: write usages to you to TODO: write the purpose

<uses-permission
    android:name="android.permission.QUERY_ALL_PACKAGES"
    tools:ignore="PackageVisibilityPolicy,QueryAllPackagesPermission" />
We will query all packages when TODO: write usages to TODO: write the purpose

<uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />
We will request delete packages when you want to uninstall this application or some other application from the user interface to provide better uninstall experience to you.

To learn more about your privacy, please see the user agreement and the privacy policy.
